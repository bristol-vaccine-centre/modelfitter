# Convert a label size from points to ggplot units

Labels like geom_text are in a random unit size which is only
mysteriously connected to the size of text on axes

## Usage

``` r
.gg_label_size(pts)
```

## Arguments

- pts:

  label size in points
